export enum GenderEnum {
  Female = "Female",
  Male = "Male",
  Unknown = "unknown",
}

export interface LocationEnum {
  name: string;
  url: string;
}

export enum SpeciesEnum {
  Alien = "Alien",
  Human = "Human",
}

export enum StatusEnum {
  Alive = "Alive",
  Dead = "Dead",
  Unknown = "unknown",
}
